<?php

namespace mc;

/**
 * Attribute marker for route handlers.
 *
 * Used by the router reflection scanner to register functions
 * and static methods as route callbacks.
 */
#[Attribute]
class route {
    
}
